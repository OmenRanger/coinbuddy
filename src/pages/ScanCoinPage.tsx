import { Card } from '../ui/Card';

export function ScanCoinPage() {
  return (
    <Card>
      <h1 className="font-serif text-3xl font-bold">Scan Coin</h1>
      <p className="mt-2 text-lg">Photo capture arrives in a later phase.</p>
    </Card>
  );
}
