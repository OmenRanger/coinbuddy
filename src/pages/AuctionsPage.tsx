import { Card } from '../ui/Card';

export function AuctionsPage() {
  return (
    <Card>
      <h1 className="font-serif text-3xl font-bold">Auctions</h1>
      <p className="mt-2 text-lg">Saved auction sources arrive in a later phase.</p>
    </Card>
  );
}
