import { Card } from '../ui/Card';

export function ArchivePage() {
  return (
    <Card>
      <h1 className="font-serif text-3xl font-bold">Archive & Reports</h1>
      <p className="mt-2 text-lg">Exports and reports arrive in a later phase.</p>
    </Card>
  );
}
